# 安心 App 多语言支持计划

- [ ] 新增 locale 解析与消息表测试，先让它失败
- [ ] 实现 `src/i18n/*` 轻量文案层与系统语言映射
- [ ] 扩展 `useAppStore` 支持 `locale` 与 `localeMode`
- [ ] 将现有页面文案和 tab 标题迁移到 `t(key)`
- [ ] 在“我的”页增加语言切换入口
- [ ] 运行 `pnpm test -i` 与 `pnpm check:type`
