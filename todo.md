# 当前暂停记录

更新时间：2026-04-23

## 当前状态

- 当前工作停留在 `brainstorming` 设计阶段，尚未进入实现。
- 已阅读：
  - `PRD_身后事务托管App.md`
  - `UXUI_安心_阶段1_体验策略与信息架构.md`
- 已启用过一次可视化辅助，但仅用于展示信息架构草图；正式设计文档尚未落盘。
- 仓库当前没有实现代码，也没有设计 spec 文档。

## 已确认的产品结论

### 产品定位

- 只考虑 `APP`，计划采用 `React Native + Expo`。
- 技术栈方向：`TypeScript + pnpm + Expo UI`。
- `iOS` 优先，同时兼顾 `Android`。
- 产品不是高频工具，核心目标是：让用户在有限时间和有限专业知识下，也能完成可执行的托付预案。

### MVP 范围

- `MVP` 必须同时包含三部分最小闭环：
  - 线下事务托付
  - 线上事务处理
  - 触发时机判断
- 触发后阶段选择的是：
  - 平台内推进任务状态和协作
  - 更高级的官方流程/半自动执行，后续单独写到 `.todos/prd_3.md`
- `.todos/prd_3.md` 目前尚不存在，后续需要补写。

### 用户与场景

- 不按财富、身份做主用户拆分。
- 核心判断标准是：用户是否需要在突发失联/死亡后，把事务有序交接出去。
- 产品要尽量简单，不能假设用户有很多时间或专业知识。

### 地域与法域

- 当前不绑定单一国家地区，按“中性国际版”设计。
- 但每个用户都需要填写 `所在国家/地区`，为后续差异化法律声明和本地化能力预留基础。

### 协作方式

- 联系人/协助人不作为完整登录用户体系设计。
- 协助方通过 `协助码` 进入 APP 内受限页面。
- 协助方只能看到自己被委托的内容。

### 核心产品模型

- 推荐并已确认采用 `预案型产品`。
- `预案` 是最小生效单位。
- 一个预案应包含：
  - 事项
  - 文件
  - 协助人
  - 触发规则
  - 执行动作
- `1 个预案 + 触发规则` 完成即可视为生效。
- 联系人不是全局必填；如果预案只包含线上事务，可不需要联系人。
- 用户可以创建多个预案。
- 每个预案拥有自己的：
  - 事项
  - 文件范围
  - 协助人
  - 触发规则
- 资源组织方式采用混合模式：
  - 用户先在预案内创建
  - 后续可复用已有联系人/文件

### 预案创建方式

- 首次创建预案采用：
  - 先回答几个问题
  - 系统自动推荐模板
- 事项结构采用两层：
  - 上层是 `预案/托付方案`
  - 下层是具体 `事项`

### 线上事务边界

- `MVP` 不允许托管第三方账号敏感访问信息。
- 只保存：
  - 账号线索
  - 处理说明
  - 官方入口
  - 所需文件
- 对“线上事务直接执行”的理解不能粗暴统一成“脚本”。
- 更合理的抽象是 `执行动作`，至少区分：
  - 通知动作
  - 文件释放动作
  - 连接器动作
  - 脚本动作
- `MVP` 先只做结构化动作，不开放用户自定义脚本。
- 最终目标需要支持用户自定义脚本，但不进入首版。
- 平台预置脚本未来是便于非专业用户使用的内置能力。

### 失联 / 死亡 预案定义

- 产品定义上，`失联` 和 `死亡` 可以区分。
- 但在 `MVP` 执行模型中，两者本质采用相同处理方式。
- 原因：
  - 平台不一定能获取可靠死亡信息
  - 预案中的线上/线下事务模型已经能够承接后续处理
- 因此：
  - 不额外设计一套“死亡专用执行流程”
  - 区别主要放在模板文案和说明层

### 触发规则

- 每个预案的触发规则采用混合模式：
  - 先选模板预设
  - 再允许高级用户微调
- 失联确认采用主动确认机制。
- 确认渠道：
  - `App 内确认`
  - `短信/邮件确认`
- 短信/邮件内容允许用户在 APP 中配置。
- 连续未主动确认的次数由用户自行设置。
- 上限为 `5 次`。
- 达到设定次数后，预案进入执行阶段。

### 执行阶段规则

- 预案达到触发条件后，直接进入 `执行阶段`。
- 如果存在 `线下事务`：
  - 通过预留方式通知协助人
- 如果存在 `线上事务`：
  - 按用户预设规则自动推进

### 已确认的顶层 IA 方向

- 顶层结构建议并已得到认可：
  - `首页`
  - `预案`
  - `执行`
  - `我的`
- 产品主心智不是模块工具，而是“完整托付单元”。

## 尚未完成的工作

### 还没输出的设计内容

- 详细的 `预案模型`
- `首次创建主流程`
- `首页 / 预案 / 执行 / 我的` 四大信息架构细化
- 协助码进入后的受限页面结构
- `MVP` 与长期目标的能力分层表
- 面向 `iOS 优先 + Expo` 的交互和技术约束回收

### 按 brainstorming 技能，后续仍需继续的流程

1. 正式呈现设计第 2 部分：
   - `预案模型 + 首次创建主流程`
2. 再呈现设计第 3 部分：
   - 信息架构细化
   - 执行阶段结构
   - 协助模式结构
3. 等用户确认完整设计
4. 将设计写入：
   - `docs/superpowers/specs/YYYY-MM-DD-<topic>-design.md`
5. 进行 spec 自检
6. 让用户 review spec
7. 再进入 `writing-plans` 阶段，不直接开始实现

## 下次接手时建议先做的事

1. 先阅读本文件 `todo.md`
2. 再快速回看：
   - `PRD_身后事务托管App.md`
   - `UXUI_安心_阶段1_体验策略与信息架构.md`
3. 继续从“设计第 2 部分：预案模型 + 首次创建主流程”开始
4. 不要直接写代码，先完成设计确认和 spec 落盘

## 补充说明

- 可视化辅助服务器本次曾成功启动，但只是临时会话；换电脑后无需依赖本次地址，重新启动即可。
- 浏览器草图文件位于 `.superpowers/brainstorm/...` 临时目录，仅作为过程产物，不是正式设计文档。
